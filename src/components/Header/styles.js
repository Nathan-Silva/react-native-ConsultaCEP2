import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({

    container: {
        flexDirection: 'row',
        backgroundColor: '#ffff',
        alignItems: 'center',
        height: 50
    },

    text: {
        fontSize: 28,
        fontFamily: 'arial',
        color: '#008000',
        fontWeight: 'bold'
    }

})